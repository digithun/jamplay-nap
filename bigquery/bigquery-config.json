{
   "BIGQUERY_INSERT_BODY_TEMPLATE": {
      "method": "insertRowsAsStream",
      "params": {
         "datasetId": "${datasetId}",
         "tableId": "${tableId}",
         "projectId": "${projectId}"
      }
   },
   "BIGQUERY_QUERY_COUNT_CONTENT": {
      "method": "query",
      "params": {
         "datasetId": "${datasetId}",
         "tableId": "${tableId}",
         "projectId": "${projectId}",
         "query": "select contentId, count(*) as count from ${tableId} where contentType = '${contentType}' and contentId = '${contentId}' group by contentId order by count desc"
      }
   },
   "BIGQUERY_QUERY_VERIFY_EXIST": {
      "method": "verify"
   },
   "BIGQUERY_CACHE_TTL_SEC": 60
}